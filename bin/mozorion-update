#!/bin/bash

MY_DIR="$( cd -P "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source "$MY_DIR/_mozinclude"

cd "$MY_REPO_DIR"
export FIREFOX_HOME="$MY_REPO_DIR"

browser/devtools/sourceeditor/orion/Makefile.dryice.js
